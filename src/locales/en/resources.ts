import common from 'locales/en/common.json';
import navigation from 'locales/en/navigation.json';
import cats from 'locales/en/cats.json';
import about from 'locales/en/about.json';

export const resources = {
  common,
  navigation,
  cats,
  about,
} as const;
