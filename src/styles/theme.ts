const tintColor = '#2f95dc';

const light = {
  text: '#000',
  background: '#fff',
  tint: tintColor,
  tabIconDefault: '#ccc',
  tabIconSelected: tintColor,
};

const dark = {
  text: '#fff',
  background: '#000',
  tint: tintColor,
  tabIconDefault: '#ccc',
  tabIconSelected: '#fff',
};

export { light, dark };
