const light = {
  surface: '#fafafa',
  onSurface: '#000',
  border: '#b8c2ca',
};

const dark = {
  surface: '#222222',
  onSurface: '#fff',
  border: '#b8c2ca',
};

const colors = { light, dark };

export default colors;
