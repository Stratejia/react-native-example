const light = {
  divider: '#eee',
  border: '#eee',
};

const dark = {
  divider: 'rgba(255,255,255,0.1)',
  border: 'rgba(255,255,255,0.1)',
};

const colors = { light, dark };

export default colors;
